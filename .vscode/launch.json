{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug FlowState (Direct)",
            "program": "${workspaceFolder}/target/debug/flowstate",
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": {
                "type": "cargo",
                "command": "build"
            }
        }
    ]
}